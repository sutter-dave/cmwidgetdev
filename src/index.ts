import {checkboxPlugin} from "./checkboxWidget"
import {regexpLinter} from "./linter"
import {images} from "./blockWidget"
import {codeEvaluator} from "./codeEvaluator"

export {checkboxPlugin}
export {regexpLinter}
export {images}
export {codeEvaluator}

export function doubler(x: number) {
    return 2*x
}